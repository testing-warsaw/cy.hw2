name : Cypress Tests
 on: [push]
 jobs:
 cypress-run:
 runs-on: ubuntu-latest
 steps:
  - name: Checkout Repository cy.hw2
    uses: uses:actions/checkout@v4
    
  - name: Install Dependencies cy.hw2
     run: npm install  

  - name: Run Cypress Tests cy.hw2
     run: npm run "cy:open:chrome"